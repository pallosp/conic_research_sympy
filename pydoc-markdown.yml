loaders:
  - type: python
    search_path: [src/lib]

processors:
  - type: filter
    skip_empty_modules: true

renderer:
  type: markdown
  filename: docs/api.md
  render_toc: true
  source_linker:
    type: github
    repo: pallosp/conic_research_sympy
  source_format: "[(source)]({url})"
